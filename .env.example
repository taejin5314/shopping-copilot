# ── Required for IKEA adapter ──
MCP_URL=http://localhost:3000
# MCP_API_KEY=          # optional, if ikea-mcp requires auth

# ── Optional ──
PORT=4000
# ANTHROPIC_API_KEY=    # enables LLM synthesis; omit for deterministic fallback
